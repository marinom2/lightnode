{
  "name": "lightnode-desktop",
  "version": "0.1.3",
  "private": true,
  "description": "LightNode desktop shell - true one-click LightChain AI worker onboarding (Tauri v2).",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  }
}
